#include <iostream>
using namespace std;

// Thanks to using set we do not need to worry about duplicates, but counting
// occurences becomes a problem
// With vector it would be easier to count occurences, but we would have to
// do extra work to avoid printing the same line multiple times

int main(void)
{
    return 0;
}