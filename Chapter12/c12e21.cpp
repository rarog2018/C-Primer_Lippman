#include <iostream>
using namespace std;

/*
std::string& deref() const
{ return (*check(curr, "dereference past end"))[curr]; }
*/
// IMO the two statement version is way more clear, at first i had no idea what
// was going on, only after a moment i realized that this is equvalent to
// (*p)[curr].
// Although seeing it n-th time makes me think that it is worth getting used to
// writting statements like this, because it saves space

int main(void)
{
    return 0;
}