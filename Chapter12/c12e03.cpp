#include <iostream>
using namespace std;

//////////////////////////////////////////////////////////////////////////////
// const versions of push_back() and pop_back() should not be added to the
// class StrBlob. We should not be able to change a const object, and if we
// would add those versions of these functions we would be able to do so.
// I cannot think of a single reason to do it.

int main(void)
{
    return 0;
}