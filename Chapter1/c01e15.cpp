#include <iostream>
using namespace std;
int main(void)
{
    // syntax errors
    // cout << hello   // missing quotation marks around hello and semicolon
    // at the end of the statement

    // type errors
    // int a = "semicolon"; // we try to assign char* to an int

    // declaration errors
    // int b = c + d; // c and d are not declared
    return 0;
}