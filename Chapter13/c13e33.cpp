#include <iostream>
using namespace std;

// The parameter is a reference to the Folder object, because we want to work
// on the original Folder object, not the copy of it. It is not const, because
// we want to change one of the object's members.

int main(void)
{
    return 0;
}