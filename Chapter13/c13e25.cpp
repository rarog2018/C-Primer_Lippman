#include <iostream>
using namespace std;

// copy constructor: should initialize 'data' member by allocating a new memory
// constructed (copied) from the memory to which the original object's shared_ptr
// points

// copy-assignment operator: it does not need to free the memory because the
// shared_ptr does this automatically, so we have to just do the same as in
// copy constructor

int main(void)
{
    return 0;
}