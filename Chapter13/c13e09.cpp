#include <iostream>
using namespace std;

// destructor is a member function with the name of the class prefixed by a
// tilde, it has no return value and takes no parameters

// synthesized destructor destroys each member in reverse order from the order
// in which they were initialized

// destructor is synthesized when the class does not explicitly define one

int main(void)
{
    return 0;
}