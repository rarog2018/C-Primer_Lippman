#include <iostream>
using namespace std;

// copy-assignment operator is a function that redefines the meaning of the
// assignment operator '='
// ClassName& operator=(const ClassName &); // example copy-assignment operator

// it is used when we assign a value to an object of a given type (class)

// synthesized copy-assignment operator is a version of copy- or move-assignment
// operator created by the compiler for classes that do not explicitly define
// a copy-assignment operator
// it memberwise assigns the right-hand operand to the left
// it is used in same cases as copy-assignment operator

int main(void)
{

    return 0;
}