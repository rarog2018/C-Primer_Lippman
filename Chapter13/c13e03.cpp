#include <iostream>
using namespace std;

// Copying both StrBlob and StrBlobPtr has the same effect: smart pointers to
// the dynamically allocated memory are copied. The contents of that memory itself
// is not copied.

int main(void)
{
    return 0;
}