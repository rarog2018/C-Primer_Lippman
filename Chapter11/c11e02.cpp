int main(void)
{
    /*
    set - when we want to know if the particular value is present

    map - when we want to create a dictionary

    vector - when performance matter and we do not know which container to use :)

    deque - when we need to quickly and efficiently insert and delete the 
    elements at the beginning and at the end of a container

    list - when we do not need random access and we do not know how many 
    elements there will be and (or) we need to insert and delete the elements
    at a random position in the container
    */
    return 0;
}