#include <iostream>
#include <map>
#include <vector>
using namespace std;

int main(void)
{
    /*
    1. Vector is defined by one type, while map is defined by two types - the
    type of the key and the type of the associated value.
    vector<int> ivec;
    map<string, int> simap;

    2. In map a given key may appear only once, while in vector its values can
    occur multiple times.

    3. In map each key is associated with a particular value, and this value
    is found by the key, while the values of vector are found by their position.
    */
    return 0;
}