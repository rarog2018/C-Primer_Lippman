#include <iostream>
using namespace std;

// if (read(input, total))
// for example in this line read expects istream object, but we are passing an
// ifstream object, it is possible because ifstream inherits from istream
// these classes are constructed in such way that it is safe to do, and the
// function does its job as expected

int main(void)
{
    return 0;
}