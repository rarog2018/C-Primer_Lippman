#include <iostream>
using namespace std;

// Query_base class is an abstract class so we cannot directly create an object
// of its type
// What happens to the objects that have the type of the class derived from the
// Query_base depends on the definitions of such classes

int main(void)
{
    return 0;
}