#include <iostream>

using namespace std;
int main(void)
{
    int a, b;
    cout << "Type two integers: ";
    cin >> a >> b;
    cout << "\n"
         << a << " / " << b << " = " << a / b << endl;
    return 0;
}