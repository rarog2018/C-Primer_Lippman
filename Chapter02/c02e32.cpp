#include <iostream>
using namespace std;

int main(void)
{
    // it is illegal, because pointer cannot be initialized from an int value
    // int null = 0, *p = null;

    // we can just define p as a nullptr
    //
    // int *p = 0;
    // int *p = nullptr;

    return 0;
}