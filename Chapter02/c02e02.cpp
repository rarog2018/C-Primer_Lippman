#include <iostream>
using namespace std;
int main(void)
{
    // it depends how large is the principal and payment, but i would use
    // double just because float may not have enough precision,
    // there are some sites that suggest that the rate precision is 4 digits
    // after the dot, but i found one where the pattern is: "0.05/12 = 0.004167."

    double rate, principal, payment;
    return 0;
}