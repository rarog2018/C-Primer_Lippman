#include <iostream>
using namespace std;

// overloaded operators do not preserve the order of evaluation and/or short-
// curcuit evaluation properties of the built-in operators (especially noticeable
// with logical AND and logical OR operators)
// overloaded operator if it is a member function, has one less parameter than
// the number of operands
// built-in operator cannot be call as a function
// not every built-in operator can be overloaded

// both oberloaded and built-in operators are functions that define what happens
// when the given operator is used

int main(void)
{
    return 0;
}