#include <iostream>
using namespace std;

// New standard added lambdas, because they are very convenient to use in many
// situations especially with the generic algorithms.
// I would use lambda over function-object most of the times
// The only situations in which i would define a function-object class would be
// if there were some extra processing necessary, but i can't think of such
// situation in the context of generic algorithms.

int main(void)
{
    return 0;
}