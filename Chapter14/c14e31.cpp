#include <iostream>
using namespace std;

// All members of StrBlobPtr can be copied, assigned and destroyed correctly
// with the synthesized members, so there is no need to define them

// members of StrBlobPtr:
// shared_ptr<vector<string>> check(size_t i, const string &msg) const;
// weak_ptr<vector<string>> wptr;
// size_t curr; // current position within the array

int main(void)
{
    return 0;
}