#include <iostream>
#include <vector>

using namespace std;
int main(void)
{
    // Because it is the iterator which has the effect of adding elements to
    // a container, the algorithm itself does not.
    // Algorithms operate on iterators not containers. Algorithm cannot directly
    // add or remove elements.
    return 0;
}