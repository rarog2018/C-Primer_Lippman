#include <iostream>
#include <algorithm>
using namespace std;

int main(void)
{
    /*
    copy(beg, end, destination) requires input iterators for first two parameters
    and output iterator for third - destination - parameter

    reverse(first, last) requires bidirectional iterator for both first and last
    parameters

    unique(first, last) requires forward iterator for both first and last 
    parameters
    */
    return 0;
}