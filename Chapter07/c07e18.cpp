#include <iostream>
using namespace std;

// Encapsulation means that the code after the 'private' specifier is accessible
// only to the member functions of the class.
// It is useful, because thanks to that the programmer can "force" the user to
// use the class in a certain way. The user won't mess up with the encapsulated
// members, and he doesn't even need to know how the code is implemented to use
// the class.

int main(void)
{
    return 0;
}