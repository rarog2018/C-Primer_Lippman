#include <iostream>
using namespace std;

// Yes, 'Screen' can safely rely on the default versions of copy and assignment
// because all of its members are of type 'string' which defines these operations
int main(void)
{
    return 0;
}