#include <iostream>
using namespace std;

// pros: it might be more clear that certain line of code is called on an object
// this->member_fnc() instead of member_fnc()
//
// cons: it seems pointless when you know when the 'this" pointer is used implicitly

int main(void)
{
    return 0;
}